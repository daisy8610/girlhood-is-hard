name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 3 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Query database to keep project active
        run: |
          curl -sf "https://krwbiyrzspdroarnqykb.supabase.co/rest/v1/profiles?select=id&limit=1" \
            -H "apikey: sb_publishable_iKjlGqxvKeJ6rxupenT8Gw_S8fI6VoG"
