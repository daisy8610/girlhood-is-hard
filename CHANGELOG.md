# CHANGELOG

## 2026-09-16（第十九次）
標題字體從思源宋體（Noto Serif TC）換成昭源圓體（Chiron GoRound TC）：

- 用到的地方：網站名稱、各頁大標題、詢價頁類別標題與診所名稱、筆記 Markdown 標題、載入中畫面
- CSS class 從 `.serif` 改名為 `.title-font`（新字體不是宋體了）
- 不再載入思源宋體，原本載入但沒用到的 900 字重也一起拿掉；昭源圓體只載入有用到的 500、700

## 2026-09-16（第十八次）
修正修改資料時可能沒有真的存進資料庫的潛在 bug：

- 原本 `update` 在 React 的 setState 更新函式裡順便算出要存的內容，React 延後執行時，送進資料庫的會是 undefined，結果是「更新失敗」並重新讀取
- 改成用 `useRef` 保存四種資料的最新版本，新增、修改、刪除、重新讀取都同步更新它，修改時從這裡先算好內容再存
- 用瀏覽器實測（假資料庫）：非點擊事件中連續修改三次，舊版三次都送出空內容，新版三次內容都正確，連續修改同一筆也不會互相蓋掉
- 實際點按鈕修改時 React 通常會立刻計算，所以之前沒有出現過這個錯誤

## 2026-09-16（第十七次）
拆分 `App.jsx`（只搬位置，邏輯不變）：

- `src/lib/useAppData.js`：資料讀取、新增/修改/刪除、總覽金額、備份匯出匯入、清空、預算上限
- `src/lib/useGoogleCalendar.js`：Google 日曆連結、解除、同步、授權過期處理
- `src/components/AppShell.jsx`：側邊欄、頁首、底部導覽、toast，分頁清單和頁面標題也搬進來
- `App.jsx` 從 359 行減到約 100 行，只負責登入狀態、toast、全螢幕狀態畫面和分頁切換
- 用無頭 Chrome 在手機與桌機寬度各比對 379 個元素，和拆分前完全相同；並逐行比對確認原本的邏輯都有搬過去

## 2026-09-16（第十六次）
樣式改用獨立 CSS 檔，第 4 批（最後一批）：

- `App.jsx`（載入畫面、側邊欄、頁首、錯誤與缺資料提示、底部導覽、toast）、`AuthScreen.jsx`、`SettingsPage.jsx`、`NotesTab.jsx`、`lib/markdown.jsx` 改成 `styles.css` 裡的 class
- 至此全站只剩隨資料變動的樣式（分類色、進度條寬度、長條高度、明細卡位置）留在元件裡
- 用無頭 Chrome 在手機寬度與桌機寬度各比對 379 個元素（含未設定連線、載入中、同步中、讀取錯誤、缺資料、寫入中、toast、還原預覽、清空確認、登入錯誤與註冊模式、筆記展開與各種 Markdown 區塊），完全相同

## 2026-09-16（第十五次）
樣式改用獨立 CSS 檔，第 3 批：

- 總覽頁的存摺卡片、分類小卡（`Overview.jsx`）、年度支出比較（`TrendChart.jsx`）、子分類排行（`SubcategoryRanking.jsx`）、儲值金（`VouchersPanel.jsx`）改成 `styles.css` 裡的 class
- 留在元件裡的只有隨資料變動的部分：分類色、進度條寬度、長條高度、明細卡左右位置
- 用無頭 Chrome 比對改前改後 297 個元素（含趨勢圖明細卡展開在第一、中間、最後一根柱子的狀態），完全相同

## 2026-09-16（第十四次）
樣式改用獨立 CSS 檔，第 2 批：

- `ui.jsx`（標籤、區塊標題、新增按鈕、切換按鈕、搜尋框、下拉建議、通用表單）、`QuotesTab.jsx`、`SpendingTab.jsx` 的 inline 樣式全部改成 `styles.css` 裡的 class
- 只剩分類色圓點的顏色留在元件裡（隨資料變動）
- 用無頭 Chrome 比對改前改後 305 個元素的實際樣式，完全相同

## 2026-09-16（第十三次）
樣式改用獨立 CSS 檔，第 1 批：

- 新增 `src/styles.css`，把原本寫在 `App.jsx` 裡的全站 CSS 字串搬過去，build 時會另外產生 `app.css`，`index.html` 載入它
- Google Fonts 改在 `index.html` 用 `<link>` 載入
- 修正粉色時期的遺留：手機狀態列顏色 `theme-color` 從粉紅 `#B5445B` 改成 `#FDFCFC`，頁面底色從 `#EAE0D6` 改成 `#FDFCFC`
- README 補上 `app.css`、`styles.css` 說明

## 2026-09-16（第十二次）
- 字型載入拿掉已經沒用到的細體（300），Noto Sans TC 和 Inter 少下載一個字重

## 2026-09-16（第十一次）
繼續整理樣式結構，畫面基本不變：

- 顏色：元件裡直接寫的主題色（黑、邊框灰、深灰、淺灰、表面米色、底色）全部改成 `--ed-*` CSS 變數；順便修正 `--ed-ash` 還停在舊淺灰 `#A59F97` 的問題，並把 fallback 用的 `#999` 併進 `--ed-ash`。白色 `#fff` 和分類色 `MAIN_COLORS` 維持原樣
- 圓角：9 種收斂成 `--r-sm`（8px）、`--r-md`（12px）、`--r-lg`（16px）加藥丸形，原本 4/7px 變 8、10px 變 12、14/20px 變 16
- 藥丸切換按鈕：詢價頁和子分類排行各自寫的版本，合併成 `ui.jsx` 的共用 `Chip`（`CategoryChips` 也改用它）

## 2026-09-16（第十次）
整理字級結構：原本散在 12 個檔案、約 100 處的數字字級，統一改成 `App.jsx` 全站 CSS 裡的 8 個變數（`--fs-xs` 到 `--fs-4xl`），之後調整字級只要改那幾行。

- 原本 13 種大小收斂成 8 種，少數地方有 1px 差異（17→18、19→18、20/22→21、25/27→26），畫面幾乎不變

## 2026-09-16（第九次）
全站字體放大、加粗、加深，參考 glp1tw.com 的易讀性：

- 字級整體放大一階（11px 以下→12、12→13、13→14、14→15，更大的標題各加 1px）
- 細體（300）全部改成一般粗細（400）
- 輔助文字的淺灰 `#A59F97` 加深成 `#7D776F`，提高對比
- 詢價列表每行上下留白加大；顏色、版面、功能都沒有改

## 2026-09-16（第八次）
拿掉詢價比較頁的「轉為消費紀錄」功能。

- 移除每筆詢價的 ➜🧾 按鈕，以及 `App.jsx` 裡只剩這裡在用的 `convertQuoteToExpense`、`convertToExpense`
- 之前已經轉過去的消費紀錄不受影響

## 2026-09-16（第七次）
「舊報價」小標讓畫面太亂，改回超過 6 個月的詢價整行變淡。

## 2026-09-16（第六次）
超過 6 個月的詢價不再整行變淡（太難閱讀），改成在名稱旁加上灰框的「舊報價」小標。

## 2026-09-16（第五次）
詢價比較頁改版，參考 glp1tw.com 的版面，讓比價更好用：

- 有填單位數量時自動算單位價（例如 `$80/單位`），分組比價改成依單位價排序，沒填數量的排在最後
- 同一產品近半年內單位價最低的報價標上「最划算」（要至少兩筆可比較；超過半年的舊報價不參加）
- 搜尋框上方新增療程類別按鈕，只顯示有資料的類別，可以和搜尋同時使用
- 新增「依診所」檢視：每間診所一張卡片，列出所有詢價項目和最近詢價時間
- 日期旁邊加上「X 天前／X 個月前」，超過 6 個月的報價整行變淡
- 依日期列表中沒填產品的紀錄改成顯示「未標示」
- 所有功能都從現有欄位計算，資料庫和新增表單都沒有改

## 2026-09-16（第四次）
分類識別色從莫蘭迪奶茶色換成蜜桃乳霜色，奶茶色看起來太土。

- 醫美 `#E8A6AE`、頭髮 `#A0BCDB`、美容 `#F2C9A0`、指甲 `#AED3B8`

## 2026-09-16（第三次）
分類識別色改成莫蘭迪奶茶色，跟黑白米色主題比較搭。

- 醫美 `#D9718A` → `#C99A9E`、頭髮 `#2A78D6` → `#8EA4BA`、美容 `#D68A2E` → `#CDAA7D`、指甲 `#1BAF7A` → `#9DB59A`
- 另一組備選的莫蘭迪粉霧色寫在 `src/lib/format.js` 的註解裡，之後想換可以直接替換

## 2026-09-16（第二次）
新視覺風格第二階段：把剩下的分頁全部換掉，粉色系正式全面退場。

- 換掉：消費紀錄、詢價比較、筆記區、設定，以及總覽頁裡的年度支出比較、子分類排行、儲值金面板
- 共用元件（`ui.jsx` 的表單、按鈕、標籤、搜尋框等）統一改成 hairline 邊框、藥丸形按鈕、純黑/米色配色
- 全站掃過一輪，確認沒有殘留的舊粉色色碼（分類識別用的彩色圓點例外，那是資料本身的顏色，不是主題色）

## 2026-09-16
開始換新的視覺風格（黑白／米色系極簡編輯風，取代粉色系），第一階段：

- 新增設計 tokens（eggshell 底色、taupe 表面、純黑文字、hairline 邊框），字體改用 Inter（中文仍是 Noto Sans TC，Inter 沒有中文字），標題原本想用的 Waldenburg 是商業字體無法免費嵌入，沒有用
- App 外殼加上 RWD：手機維持底部導覽列，寬螢幕（≥900px）改成左側常駐側邊欄，內容區也跟著變寬，不再是被拉寬的手機版面
- 登入頁、總覽頁換成新風格（扁平卡片、hairline 邊框、純黑進度條、藥丸形按鈕）
- 消費紀錄／詢價比較／筆記區／設定目前還是舊的粉色風格，之後分批換

## 2026-09-15
修正詢價新增功能一直沒被發現的兩個 bug，並調整療程類別：

- `quoteToDb`（`db.js`）沒有寫入資料庫要求必填的 `treatment_name` 欄位，導致從畫面「新增詢價紀錄」實際上一直會失敗（現有的 78 筆詢價全部是 2026-07-16 從 Notion 一次性匯入的，不是透過畫面新增的，才一直沒被發現）。補上 `treatment_category + product` 組合寫入
- `quoteToDb` 還手動塞值給 `unit_price`，但這欄位在資料庫裡其實是自動計算的 generated column，手動寫入一樣會被拒絕。拿掉這行，讓資料庫自己算
- 療程類別「皮秒」改名「膚質雷射」，範圍變廣一點；拿掉一直沒人用過的「複合療程」類別；既有 2 筆「皮秒」（蜂巢皮秒）資料一併改標籤成「膚質雷射」
- 直接寫入 7 家診所的「青萃光」詢價比價資料（芮美娣、辰美學、怡康、依定美、藏琢、上立美學、沐西醫美），歸在「膚質雷射」類別下

## 2026-09-11（第二次）
拿掉筆記頁的「年度目標與預算分配策略」卡片：內容一直是空的沒寫過，跟預算計畫一起變得沒有意義。移除 `NotesTab.jsx` 的 `StrategyCard`、`App.jsx` 的 `saveStrategy`、`db.js` 查 `profiles.annual_strategy` 那段；筆記清單本身完全沒受影響。

## 2026-09-11
把「筆記」從「更多」選單裡拉出來變成獨立分頁，「更多」直接變成「設定」：

- 底部導覽從「總覽／紀錄／詢價／更多」變成「總覽／紀錄／詢價／筆記／設定」，點「設定」直接進設定頁，不用先進「更多」選單再點一次
- 刪掉整個 `MoreMenu.jsx`（選單畫面跟「‹ 返回」外框都用不到了）
- `MoreMenu.jsx` 原本頁尾那段「資料只存在 Supabase…記得下載備份」的提醒文字，移到設定頁的「關於」卡片下面，沒有不見
- 「請到『更多 → 設定』匯入」這類已經過時的提示文字一併改掉
- 順手補上 README 裡漏掉的更新：拿掉已刪除的 `BudgetTab.jsx`／`budget_plans`／「更多」相關描述，補上一直沒寫進去的 `SubcategoryRanking.jsx`，功能清單改成目前實際的樣子

## 2026-09-10（第六次）
- 日曆同步觸發方式改成手動勾選：消費紀錄新增表單加「同步到 Google 日曆」勾選框（預設不勾），勾了才同步，不管日期是過去、今天還是未來；拿掉原本「只有未來日期才同步」的規則，因為補記過去的預約時那條規則反而擋住同步

## 2026-09-10（第五次）
- 日曆同步的說明欄加上金額：格式變成「備註 $金額」換行「#漂亮」，備註留空時就只有「$金額」那一行，金額格式跟 app 內顯示一致（`$1,200`）。`google-calendar-sync` 已重新部署

## 2026-09-10（第四次）
- 日期/時間欄位的底色改回白色，跟其他欄位一致：上一版只是拿掉米色設定，沒有明確補回白色，結果透出表單本身的米色底，這次補上明確的 `#fff`

## 2026-09-10（第三次）

消費紀錄表單細節調整：

- 時間欄位拿掉冗長說明，只留「時間」
- 日曆同步的說明欄固定加上 `#漂亮` 標籤方便搜尋，只影響同步到 Google 日曆的內容，不會寫進消費紀錄本身的備註
- 主分類改成必填（補上 *，表單也會真的擋下沒選就送出）
- 子分類的建議清單改成依目前選的主分類篩選，沒選主分類時才顯示全部
- 項目名稱的留空提示文字縮短成「留空會自動帶入」
- 日期／時間欄位換成跟主題一致的外觀（等寬字體、米色底、粉色的日曆／時鐘小圖示），不再是瀏覽器預設的樣子
- `google-calendar-sync` 已重新部署

## 2026-09-10（第二次）

消費紀錄新增表單加時間欄位、優化日曆同步：

- 新增選填的「時間」欄位，只用來給日曆同步用，不會存進消費紀錄本身
- 有填時間就同步成 1 小時長的定時行程（例如 14:00–15:00），沒填時間維持全天事件
- 項目名稱留空時的自動帶入，從「只帶子分類」改成「主分類 子分類」
- 地點、備註同步進日曆事件的地點/說明欄位這件事本來就有做，這次沒有改動
- `google-calendar-sync` Edge Function 已重新部署

## 2026-09-10

拿掉「預算計畫」功能，日曆同步改掛在消費紀錄：

- 移除「預算」分頁、`BudgetTab.jsx`、`budget_plans` 整條資料流（新增/編輯/刪除/備份還原/清空資料）
- Google 日曆同步改成在「消費紀錄」新增時觸發，且只在填的日期是未來才會同步，避免日常花費也自動塞進日曆
- 「年度預算上限」功能保留；總覽的進度條改成「今年累積支出 ÷ 上限」（原本是用預算計畫的已規劃金額），右下角格子從「已規劃」改成「剩餘額度」
- 順手清掉幾處殘留的「預算」字樣：登入頁標語、備份還原說明文字、一處過時註解

## 2026-08-31（第二次）
- 拿掉 keep-alive 排程裡的防快取參數（`_=timestamp`）：PostgREST 把不認得的參數當成欄位篩選，語法不合法直接回 400，排程反而全部失敗；確認過這個端點本來就是 `cf-cache-status: DYNAMIC`（Cloudflare 不快取），根本不需要防快取

## 2026-08-31
- keep-alive 排程頻率從每 3 天一次改成每 4 小時一次，並加防快取參數：Supabase 官方文件說明是「一週內每天都要有幾次真的打到資料庫的請求」，每 3 天一次即使真的查了資料表也不夠頻繁，導致又收到即將暫停的預警信

## 2026-08-24
- 修正 keep-alive 排程：原本打 `/auth/v1/health` 不會真的碰到資料庫，不算 Supabase 判斷的「活動」，專案照樣被暫停；改成查詢 `profiles` 資料表，確保排程真的能延續資料庫的活躍狀態

## 2026-08-12
- 新增 GitHub Actions 排程（每 3 天打一次 Supabase health 端點），避免免費方案因 7 天無活動自動暫停專案

## 2026-08-03
- 修正 Google 日曆授權過期（refresh_failed）時畫面仍顯示「已連結」的問題：現在會自動清掉失效的授權、把狀態切回未連結，並提示「Google 日曆授權已過期，請重新連結」，不會再無聲同步失敗

## 2026-07-24（第四次）
- 修日期欄位在 iOS Safari 超出框框的問題：input[type=date] 原生控制項渲染寬度有時會無視 CSS 設定值，加上 overflow:hidden 的外層容器裁掉超出部分

## 2026-07-24（第三次）
- 表單裡日期欄位改成獨立一整排，不再跟主分類擠在同一行造成手機版跑版
- 加到主畫面（PWA 全螢幕模式）時，頂部標題列補上安全區域留白，不再被手機狀態列（時間、電量）卡到

## 2026-07-24（第二次）
- 地點/子分類等有建議清單的欄位，把原本的原生 datalist 換成自己刻的下拉選單，電腦跟手機行為終於一致（原本手機版 Safari 對 datalist 支援很弱，常常只有打字建議、沒有下拉清單）

## 2026-07-24
- 消費/預算/詢價的必填欄位（項目名稱、金額、預算金額、診所名稱、價格）沒填會擋下並提示，不會再存成空白或未命名；金額欄位仍允許負數以記退款
- 詢價比較分組排序修正：0 元（贈送）品項原本被當成沒填價格排到最後，現在正確排最前面
- 詢價比較加「複製」按鈕，同一診所報多個療程不用重複打診所名稱
- 預算計畫取消勾選「已完成」時加確認提示，說明不會動到已經記下的消費紀錄，避免手滑誤解
- 消費紀錄與預算計畫的主分類清單合併成同一份共用設定，之後新增分類只需要改一處
- 修正連續快速編輯同一筆資料時，樂觀更新可能用到舊資料把前一次修改蓋掉的問題
- 還原 JSON 備份的確認畫面加風險提醒：還原中若網路中斷可能導致資料處於新舊資料都不完整的中間狀態
- db.js 補上單位數量為負數時不計算單價、以及未知資料種類時丟出明確錯誤訊息的防呆

## 2026-07-22
- 換 app 圖示（icon-180/192/512），改用粉色愛心筆記本圖案

## 2026-07-20（第四次）
- 年度支出比較長條圖：點一下柱子跳出明細卡片（年份＋各分類金額，由大到小排），邊界年份（第一根/最後一根）的卡片會貼齊邊緣不跑出畫面外
- 分類色盤全部換掉：原本頭髮/美容/指甲三色飽和度太低、色盲辨識度不合格（跑過驗證工具確認），改成更飽和好分辨的藍/橘/綠
- 全站主色從深酒紅（#8a3b4d）換成暖桃粉玫瑰（#C25B72 系列），背景改成更白淨的暖粉白，整體更亮但保留低飽和度；文字用色另外抓一版更深的色號維持對比度，跑過對比度檢查
- 長條圖改用 HTML/CSS 畫（原本是 SVG），固定寬度、頂部圓角、區塊間留白，比原本乾淨

## 2026-07-20（第三次）
- 總覽頁「近12個月趨勢」改成「年度支出比較」，以年為單位（2020～今年）疊色長條圖，之前的圖表寫死只抓近12個月，看不到更早的歷史（資料庫其實從2020年就有完整紀錄）
- 總覽頁新增「子分類佔比排行」，抓消費紀錄的子分類（玻尿酸、肉毒、除毛等）做排行榜，可切換今年／全部年度

## 2026-07-20（第二次）
- 底部導覽加「詢價」，不用再進「更多」才能點；更多選單只剩筆記區、設定
- 預算項目打勾完成後直接標記完成並自動記一筆消費紀錄，不再跳「要不要新增」的確認列；取消勾選只改狀態，不會動已經記下的消費紀錄
- 拿掉 BudgetTab 裡自帶的策略 markdown 編輯器，年度目標與預算分配策略改到「更多 → 筆記區」編輯，預算頁只顯示唯讀內容
- 詢價比較拿掉「最低價」自動標示（需要品項名稱完全一致才有效，容易失效誤導），保留依價格排序

## 2026-07-20（第一次）
- 消費/預算分頁的分類篩選＋搜尋邏輯抽成共用的 useCategoryFilter hook 和 CategoryChips 元件，不再各自重複一份
- 詢價、預算「轉為消費紀錄」的邏輯合併成共用的 convertToExpense 函式
- 修正「更新失敗：Cannot read properties of undefined (reading 'place')」：update 函式原本靠 setState 的 updater 順便算出合併後的資料，但 React 不保證那個時機一定同步執行，導致送出的資料常是 undefined。改成直接從目前陣列裡找出該筆資料再合併，這個函式是消費/預算/詢價/筆記/儲值金共用的，全部一起修好

## 2026-07-16（第十五次）
- 修正 google-calendar-sync 沒有 CORS 標頭，瀏覽器直接呼叫可能被擋掉且沒有任何錯誤訊息
- 預算項目同步日曆改成一定會跳 toast（成功或失敗都會顯示），方便排查問題

## 2026-07-16（第十四次）
- 撤回 providers 去重加 kind 比對的改動：既有店家的 kind 值可能跟新建預設值不一致，比對加 kind 反而可能把既有店家誤判成找不到、多建一筆重複的。恢復成純名稱比對（跟原本一樣），只保留 kind 值要符合資料庫限制（clinic/salon/store/other）這件事

## 2026-07-16（第十三次）
- 修正 providers_kind_check 檢查限制失敗：kind 只能是 clinic/salon/store/other，改成詢價用 clinic、其他消費地點用 other（空字串跟中文「診所」都不符合限制）

## 2026-07-16（第十二次）
- 修正新增消費/預算/儲值紀錄時「null value in column kind」失敗：providers.kind 是 NOT NULL，非詢價的地點改塞空字串而不是 null

## 2026-07-16（第十一次）
- 新增 Google 日曆自動同步：新增「預算計畫」項目時，若已連結 Google 日曆會自動建立一筆全天事件（用計畫日期），不用再手動開日曆
- 後端用 Supabase Edge Functions（google-oauth-start / google-oauth-callback / google-calendar-sync）處理 OAuth 授權與日曆寫入，refresh token 存在新表 google_calendar_tokens（RLS 保護）
- 設定頁加「Google 日曆」卡片：連結／解除連結
- 需要昕自行完成 Google Cloud Console 設定、Supabase Edge Function 部署、資料庫建表，見交接說明

## 2026-07-16（第十次）
- 消費紀錄的子分類、地點欄位改成有歷史建議的輸入框（datalist），打字時會跳出之前打過的選項
- 項目名稱留空時自動帶入子分類的值，不用重複打同樣的字

## 2026-07-16（第九次）
- 消費紀錄每筆加「複製」按鈕，搜尋到常打的那種紀錄（例如醫美除毛）直接複製帶入新增表單，日期自動改成今天，不用每次重打分類/地點

## 2026-07-16（第八次）
- providers 去重比對加上 kind，避免詢價的「診所」跟其他消費地點同名時被誤判成同一家店
- 總覽卡片加上「已完成項目：預算 vs 實際花費」的落差顯示（超支／省下多少）

## 2026-07-16（第七次）
- 修正登入頁輸入框超出卡片邊界的問題：登入頁沒吃到全域 box-sizing: border-box 樣式，導致 padding 疊加在 100% 寬度之外。直接在輸入框補上 box-sizing，並讓全域樣式改成所有畫面（含登入頁、載入中畫面）都套用，避免以後其他畫面又踩到同樣的坑

## 2026-07-16（第六次）
- 修正儲值金卡片在兩欄並排、數字加上「用一次」按鈕時可能被擠壓變形的問題，改成允許自動換行

## 2026-07-16（第五次）
- 預算計畫打勾完成後，跳出「要不要新增消費紀錄」確認（非自動新增，避免跟手動記錄重複）

## 2026-07-16（又續）
- 修正「用一次」按鈕：只在單位是「堂／次」時顯示，因為「元」是儲值金餘額，退費等變動不是單純扣 1 能表達的，這種情況請直接用編輯改金額

## 2026-07-16（再續）
- 儲值堂數面板加「用一次」按鈕，點一下直接扣 1，不用開表單改數字
- 詢價比較每筆加「轉為消費紀錄」按鈕，帶著金額/地點/品項直接跳到消費紀錄新增表單（詢價紀錄本身不會被刪除）
- 筆記編輯時加即時 markdown 預覽（左右分割，textarea 旁即時顯示渲染結果）

## 2026-07-16（續）
- BudgetTab 補上搜尋框與分類篩選，跟消費紀錄、詢價比較一致

## 2026-07-16
- 修正 Overview、BudgetTab 標題寫死 2026 的問題，改成自動取當年年份
- 移除 quotes 的 `treatment_name` 殘留欄位（寫入後從未被讀回，屬未使用欄位）
- BudgetTab 的年度策略區塊改成可編輯：內容不再寫死在程式碼裡，改存到 `profiles.annual_strategy`，透過畫面上的「編輯」按鈕修改（markdown 格式，跟筆記區一樣的渲染方式）
