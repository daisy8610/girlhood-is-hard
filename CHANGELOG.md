# CHANGELOG

每天一筆，依類型分組，只記最後結果；中間的嘗試與每次 commit 細節請看 git log。

## 2026-09-17

### 調整
- 詢價比較的檢視切換「分組比價」改名為「依產品」，跟「依診所」「依日期列表」的說法一致，避免跟上方的類別篩選按鈕搞混

## 2026-09-16

### 新功能
- 詢價比較頁改版（參考 glp1tw.com）：
  - 有填單位數量時自動算單位價（例如 `$80/單位`），分組比價改成依單位價排序，沒填數量的排在最後
  - 同一產品近半年內單位價最低的報價標上「最划算」（要至少兩筆可比較；超過半年的舊報價不參加）
  - 搜尋框上方新增療程類別按鈕，只顯示有資料的類別，可以和搜尋同時使用
  - 新增「依診所」檢視：每間診所一張卡片，列出所有詢價項目和最近詢價時間
  - 日期旁邊加上「X 天前／X 個月前」，超過 6 個月的報價整行變淡（試過改成「舊報價」小標，畫面太亂又改回來）
  - 所有功能都從現有欄位計算，資料庫和新增表單都沒有改

### 畫面
- 視覺風格從粉色系換成黑白米色極簡風：扁平卡片、hairline 邊框、藥丸形按鈕；寬螢幕（≥900px）改成左側常駐側邊欄，手機維持底部導覽
- 分類識別色改成蜜桃乳霜色：醫美 `#E8A6AE`、頭髮 `#A0BCDB`、美容 `#F2C9A0`、指甲 `#AED3B8`（莫蘭迪粉霧色當備選，寫在 `format.js` 註解）
- 全站字級放大一階、細體改成一般粗細、輔助文字的淺灰加深（`#A59F97` → `#7D776F`），詢價列表行距加大
- 字體：標題從思源宋體換成昭源圓體（Chiron GoRound TC）；金額與統計數字從 IBM Plex Mono 換成 Inter 等寬數字（`tabular-nums`）。全站剩 Inter、思源黑體、昭源圓體三套，也不再載入沒用到的字重
- 修正粉色時期的遺留：手機狀態列顏色 `theme-color` 從粉紅改成 `#FDFCFC`，頁面底色也改成 `#FDFCFC`

### 移除
- 詢價頁的「轉為消費紀錄」按鈕，以及 `App.jsx` 裡只剩它在用的 `convertQuoteToExpense`、`convertToExpense`（已轉過去的消費紀錄不受影響）

### 修正
- 修改資料時可能送出空內容、結果「更新失敗」的 bug：7/20 修過一次，7/24 為了修「連續快速編輯會蓋掉前一次修改」又改回在 setState 更新函式裡順便計算的寫法，讓問題回來了。這次改用 `useRef` 保存四種資料的最新版本，新增、修改、刪除、重新讀取都同步更新它，兩個問題一起解決。用瀏覽器實測（假資料庫）：舊版連續修改時三次都送出空內容，新版內容都正確，也不會互相蓋掉

### 內部整理（外觀不變）
- 樣式全部從元件的 inline style 搬到 `src/styles.css`（build 時產生 `app.css`，`index.html` 載入）；只剩隨資料變動的樣式（分類色、進度條寬度、長條高度、明細卡位置）留在元件裡
- 字級（`--fs-*`）、圓角（`--r-*`）、主題色（`--ed-*`）都改成 CSS 變數，放在 `styles.css` 最上面；原本 13 種字級收斂成 8 種、9 種圓角收斂成 4 種
- 三份各自寫的藥丸切換按鈕合併成 `ui.jsx` 的共用 `Chip`
- CSS class 改名：`.serif` → `.title-font`、`.mono` → `.num`
- `App.jsx` 拆成 `lib/useAppData.js`（資料讀寫、備份匯出匯入）、`lib/useGoogleCalendar.js`（Google 日曆）、`components/AppShell.jsx`（側邊欄、頁首、底部導覽、toast），`App.jsx` 從 434 行減到約 100 行
- 以上每一步都用無頭 Chrome 比對改前改後的實際樣式（手機與桌機寬度，數百個元素、各種畫面狀態），結果完全相同
- `supabase/.temp/` 加進 `.gitignore`；README 補上 `app.css`、`styles.css` 和新拆出的檔案

## 2026-09-15

### 修正
- 從畫面「新增詢價紀錄」其實一直會失敗，因為現有 78 筆詢價都是 7/16 從 Notion 一次匯入的，才沒被發現：
  - `quoteToDb`（`db.js`）沒有寫入資料庫必填的 `treatment_name`，補上 `treatment_category + product` 組合寫入
  - `quoteToDb` 手動塞值給 `unit_price`，但這欄位是資料庫自動計算的 generated column，會被拒絕，拿掉讓資料庫自己算

### 調整
- 療程類別「皮秒」改名「膚質雷射」，範圍變廣一點；拿掉沒人用過的「複合療程」；既有 2 筆「皮秒」（蜂巢皮秒）改標成「膚質雷射」

### 資料
- 寫入 7 家診所的「青萃光」詢價（芮美娣、辰美學、怡康、依定美、藏琢、上立美學、沐西醫美），歸在「膚質雷射」

## 2026-09-11

### 畫面
- 「筆記」從「更多」選單拉出來變成獨立分頁，底部導覽變成「總覽／紀錄／詢價／筆記／設定」，點「設定」直接進設定頁
- 原本「更多」頁尾那段「資料只存在 Supabase…記得下載備份」的提醒，移到設定頁「關於」卡片下面；「請到『更多 → 設定』匯入」等過時提示一併改掉

### 移除
- 整個 `MoreMenu.jsx`（選單和「‹ 返回」外框都用不到了）
- 筆記頁的「年度目標與預算分配策略」卡片：內容一直是空的，跟預算計畫一起失去意義。移除 `StrategyCard`、`saveStrategy`、`db.js` 查 `profiles.annual_strategy` 那段，筆記清單不受影響

### 文件
- README 拿掉已刪除的 `BudgetTab.jsx`／`budget_plans`／「更多」相關描述，補上漏寫的 `SubcategoryRanking.jsx`，功能清單改成實際狀況

## 2026-09-10

### 移除
- 「預算計畫」功能：移除「預算」分頁、`BudgetTab.jsx`、`budget_plans` 整條資料流（新增、編輯、刪除、備份還原、清空）
- 「年度預算上限」保留：總覽進度條改成「今年累積支出 ÷ 上限」，右下角格子從「已規劃」改成「剩餘額度」；順手清掉登入頁標語、備份還原說明、一處註解裡殘留的「預算」字樣

### 新功能
- Google 日曆同步改掛在消費紀錄：新增表單加「同步到 Google 日曆」勾選框（預設不勾），勾了才同步，日期是過去、今天或未來都可以
- 消費紀錄新增選填的「時間」欄位（只給日曆同步用，不存進消費紀錄）：有填就建 1 小時的定時行程，沒填維持全天事件
- 日曆事件的說明欄格式：`備註 $金額` 換行 `#漂亮`（備註留空就只有 `$金額`），`#漂亮` 方便在日曆裡搜尋，不會寫進消費紀錄本身的備註
- `google-calendar-sync` 已重新部署

### 調整
- 主分類改成必填（補上 *，沒選會擋下）
- 子分類的建議清單依目前選的主分類篩選，沒選主分類時才顯示全部
- 項目名稱留空時自動帶入「主分類 子分類」（原本只帶子分類），提示文字縮短成「留空會自動帶入」
- 時間欄位拿掉冗長說明，只留「時間」
- 日期／時間欄位換成跟主題一致的外觀（等寬字體、白底、主題色的日曆／時鐘小圖示）

## 2026-08-31

### 修正
- keep-alive 排程從每 3 天一次改成每 4 小時一次：Supabase 官方說明是一週內每天都要有幾次真的打到資料庫的請求，每 3 天一次不夠，又收到即將暫停的預警信
- 試過加防快取參數（`_=timestamp`），但 PostgREST 會把它當成欄位篩選而回 400，讓排程全部失敗，已拿掉；這個端點本來就是 `cf-cache-status: DYNAMIC`，不需要防快取

## 2026-08-24

### 修正
- keep-alive 排程原本打 `/auth/v1/health`，不會真的碰到資料庫，專案照樣被暫停；改成查詢 `profiles` 資料表

## 2026-08-12

### 新功能
- GitHub Actions 排程定期打 Supabase，避免免費方案因 7 天無活動自動暫停專案

## 2026-08-03

### 修正
- Google 日曆授權過期（`refresh_failed`）時畫面仍顯示「已連結」：現在會自動清掉失效授權、切回未連結，並提示「Google 日曆授權已過期，請重新連結」

## 2026-07-24

### 新功能
- 詢價比較加「複製」按鈕，同一診所報多個療程不用重打診所名稱
- 地點、子分類等欄位的建議清單，從原生 datalist 換成自己做的下拉選單，電腦和手機行為一致（手機 Safari 對 datalist 支援很弱）

### 修正
- 必填欄位（項目名稱、金額、預算金額、診所名稱、價格）沒填會擋下並提示；金額仍允許負數以記退款
- 詢價比較排序：0 元（贈送）品項原本被當成沒填價格排到最後，現在正確排最前面
- 連續快速編輯同一筆資料時，可能用舊資料蓋掉前一次修改（這次的改法後來讓 7/20 的「更新失敗」問題回來，9/16 一起修好）
- 表單的日期欄位改成獨立一整排，不再跟主分類擠在一行造成手機跑版
- 日期欄位在 iOS Safari 超出框框：原生 date 控制項有時會無視 CSS 寬度，加上 `overflow: hidden` 的外框裁掉
- 加到主畫面（PWA 全螢幕）時，頂部標題列補上安全區域留白，不再被手機狀態列擋住

### 調整
- 預算計畫取消勾選「已完成」時加確認提示，說明不會動到已記下的消費紀錄
- 還原 JSON 備份的確認畫面加風險提醒：還原中網路中斷可能讓資料停在新舊都不完整的狀態
- 消費紀錄與預算計畫的主分類清單合併成同一份共用設定
- `db.js` 防呆：單位數量為負數時不算單價，未知資料種類時丟出明確錯誤

## 2026-07-22

### 畫面
- 換 app 圖示（icon-180/192/512），改用粉色愛心筆記本圖案

## 2026-07-20

### 新功能
- 總覽頁「近 12 個月趨勢」改成「年度支出比較」：以年為單位（2020～今年）的疊色長條圖，原本寫死只抓近 12 個月，看不到更早的紀錄
- 年度支出比較點一下柱子會跳出明細卡片（年份＋各分類金額，由大到小），第一根和最後一根柱子的卡片會貼齊邊緣
- 總覽頁新增「子分類佔比排行」，可切換今年／全部年度
- 底部導覽加「詢價」，「更多」選單只剩筆記區、設定
- 預算項目打勾完成後直接標記完成並自動記一筆消費紀錄，不再跳確認；取消勾選只改狀態

### 畫面
- 分類色改成更飽和、色盲也能分辨的藍／橘／綠（跑過驗證工具）
- 全站主色從深酒紅換成暖桃粉玫瑰，背景改成暖粉白；文字色另外調深以維持對比度
- 長條圖從 SVG 改用 HTML/CSS 畫，固定寬度、頂部圓角、區塊間留白

### 移除
- 預算頁自帶的策略 markdown 編輯器，改到「更多 → 筆記區」編輯，預算頁只顯示唯讀內容
- 詢價比較的「最低價」自動標示（要品項名稱完全一致才有效，容易誤導），保留依價格排序

### 修正
- 「更新失敗：Cannot read properties of undefined (reading 'place')」：update 原本在 setState 的更新函式裡順便算出合併後的資料，React 不保證那時同步執行，送出的常是 undefined。改成直接從目前陣列找出該筆再合併，消費、預算、詢價、筆記、儲值金一起修好

### 內部整理
- 消費／預算分頁的分類篩選和搜尋抽成共用的 `useCategoryFilter` hook 和 `CategoryChips` 元件
- 詢價、預算「轉為消費紀錄」合併成共用的 `convertToExpense`

## 2026-07-16

### 新功能
- Google 日曆自動同步：新增預算計畫項目時，若已連結會自動建立全天事件。後端用 Supabase Edge Functions（`google-oauth-start`、`google-oauth-callback`、`google-calendar-sync`）處理授權與寫入，refresh token 存在新表 `google_calendar_tokens`（RLS 保護）；設定頁加「Google 日曆」卡片可連結／解除
- 消費紀錄每筆加「複製」按鈕，帶入新增表單，日期自動改成今天
- 消費紀錄的子分類、地點欄位加上歷史建議；項目名稱留空時自動帶入子分類
- 儲值面板加「用一次」按鈕，點一下扣 1；只在單位是「堂／次」時顯示（「元」的變動請直接編輯金額）
- 詢價比較每筆加「轉為消費紀錄」按鈕，帶著金額、地點、品項跳到消費紀錄新增表單
- 筆記編輯時加即時 markdown 預覽（左右分割）
- 預算計畫加搜尋框與分類篩選，跟消費紀錄、詢價比較一致
- 預算計畫的年度策略改成可在畫面上編輯，存到 `profiles.annual_strategy`
- 總覽卡片加上「已完成項目：預算 vs 實際花費」的落差（超支／省下多少）

### 修正
- 新增消費、預算、儲值紀錄失敗：`providers.kind` 不能是空值，且只接受 clinic／salon／store／other，改成詢價用 clinic、其他地點用 other。店家去重維持純名稱比對（試過加上 kind 比對，但既有店家的 kind 可能不一致，反而會建出重複店家，已撤回）
- `google-calendar-sync` 沒有 CORS 標頭，瀏覽器呼叫可能被擋掉且沒有錯誤訊息；預算項目同步日曆改成成功或失敗都會跳 toast
- 登入頁輸入框超出卡片：登入頁沒套到全域 `box-sizing: border-box`，補上並讓全域樣式套用到所有畫面（含登入頁、載入中畫面）
- 儲值金卡片在兩欄並排、加上「用一次」按鈕時可能被擠壓，改成允許換行
- Overview、BudgetTab 標題寫死 2026，改成自動取當年年份

### 內部整理
- 移除 quotes 寫入後從沒讀回的 `treatment_name` 欄位（後來發現資料庫其實要求必填，9/15 補回）
